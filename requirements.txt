# ============================================================
# Core — for this translation script only
# ============================================================
openai>=1.0.0
httpx>=0.27.0

# ============================================================
# Feature extraction pipeline (Section 4 of protocol)
# ============================================================
# Lexical diversity
lexical-diversity>=0.2

# NLP pipelines
spacy>=3.7
# After install: python3 -m spacy download en_core_web_sm
# After install: python3 -m spacy download zh_core_web_sm

# Sentiment analysis (transformers + torch are heavy; install optionally)
transformers>=4.40
torch>=2.0

# Statistics
scipy>=1.12
statsmodels>=0.14
scikit-learn>=1.4

# Visualization
matplotlib>=3.8
seaborn>=0.13

# Chinese tokenization (for lexical diversity on ZH)
jieba>=0.42

# ============================================================
# Documentation: pin with `pip freeze > requirements.txt`
# before submitting experiment for reproducibility
# ============================================================
